# OS files
.DS_Store
Thumbs.db

# Editor folders
.vscode/
.idea/

# Local environment / secrets (never commit these)
.env
.env.local
.env.*.local

# supabase-config.js is committed — it contains only the anon/publishable key which is safe for client-side code.
# NEVER commit the Supabase service_role key.

# Supabase local config (when you set it up)
.supabase/
supabase/.env

# Logs
*.log

# R history (not part of this project)
.Rhistory
